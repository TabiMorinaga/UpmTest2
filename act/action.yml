name: "Hello Actions"

jobs:
  act:
    name: hello wrold
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
       fetch-depth: 0
       
    - name: print
      run: |
        echo Hello World
        echo hello
